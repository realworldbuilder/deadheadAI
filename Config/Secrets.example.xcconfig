// Copy to Config/Secrets.xcconfig (gitignored) and fill in your key.
// Builds without it still work: the app falls back to the offline brain
// or a key pasted in Settings.
OPENAI_API_KEY = sk-your-key-here
